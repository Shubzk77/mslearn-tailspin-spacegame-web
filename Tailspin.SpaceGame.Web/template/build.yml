parameters: 
buildConfiguration: 'Release'

Steps:
-task: DotNetCoreCLI@2
displayName: 'Build the Project - ${{ parameters.buildConfiguration}}'
inputs:
  command: 'build'
  arguments: '--no-restore --configuration ${{parameters.buildConfiguration}}'
  projects: '**/*.csproj'

  -task: DotNetCoreCLI@2
  displayname: 'Publish The Project - $ {{parameter.buildConfiguration }}'
  inputs:
  command: 'publish'
  projects: '**/*.csproj'
  publishWebProjects: false
arguments : '--no-build --confoguration ${{ parameters.buildConfiguration }} --output $ ( Build.AtrifactStagingDirectory)/${{parameters.buildConfiguration}}'
zipAfterPublish: true

